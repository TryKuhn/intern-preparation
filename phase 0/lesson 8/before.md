Цель пре-ридинга — прийти с интуицией «что нельзя изменять». Около 30 минут.

1. Ничего устанавливать не нужно.
2. Прочитать по диагонали:
    - Что такое `const` в C++ — основы: `const int x = 5;`.
    - Чем `const` переменная отличается от `constexpr`.
3. Подумать над вопросом (обсудим в начале занятия): если `const` защищает от изменений, зачем в C++ нужно ключевое слово `mutable`? В каком случае у `const`-объекта может быть изменяемое состояние?
