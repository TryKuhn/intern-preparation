### A. Классический бинарный поиск (около 30 мин)

1. Реализуй все три варианта: exact, lower_bound, upper_bound. Протестируй на edge cases: пустой массив, все одинаковые, target не найден.
2. **LC 33** Search in Rotated Sorted Array: найти элемент в повёрнутом отсортированном массиве. O(log n).
3. **LC 34** Find First and Last Position: найти первое и последнее вхождение. Использовать lower_bound и upper_bound.
4. Отладь три специально сломанных бинарных поиска (предоставь студентке код с багами — off-by-one, неправильное обновление указателей, переполнение mid).

### B. Бинпоиск по ответу (около 50 мин)

1. **LC 875** Koko Eating Bananas: минимальная скорость.
2. **LC 69** Sqrt(x): целочисленный квадратный корень через бинпоиск.
3. **LC 1011** Capacity To Ship Packages Within D Days: минимальная грузоподъёмность корабля.
4. Задача: разрезать N верёвок на K одинаковых кусков максимальной длины. Бинпоиск по длине куска.

### C. Дебаг бинарного поиска (около 20 мин)

Исправь следующие реализации:
```cpp
// Баг 1:
while (lo <= hi) { int mid=(lo+hi)/2; if(a[mid]>=t) hi=mid-1; else lo=mid+1; }
// Вопрос: что ищет? Правильно ли?

// Баг 2 — бесконечный цикл:
int lo=0, hi=n;
while (lo < hi) { int mid=(lo+hi)/2; if(a[mid]<t) lo=mid; else hi=mid-1; }

// Баг 3 — переполнение:
int mid = (lo + hi) / 2;  // lo=1e9, hi=2e9
```

### Самопроверка (около 15 мин, письменно в `selfcheck.md`)

1. Почему `lo + (hi-lo)/2` а не `(lo+hi)/2`?
2. Чем lower_bound отличается от upper_bound?
3. Что такое «бинпоиск по ответу»? Какое условие нужно для применения?
4. Чем закрытый интервал [lo, hi] отличается от полуоткрытого [lo, hi) в бинпоиске?

### Критерий "сделано"
LC 875, 69, 33, 34 приняты. Три бага исправлены с объяснением.
