### A. BST реализация (около 80 мин)

1. Реализуй BST с insert, find, erase (все три случая удаления), inorder traversal.
2. Добавь `height()` — высота дерева.
3. Добавь `is_valid()` — проверка BST-инварианта рекурсивно.
4. Реализуй `floor(K key)` — наибольший ключ ≤ key.
5. Реализуй `ceil(K key)` — наименьший ключ ≥ key.

### B. Вырождение (около 20 мин)

1. Вставь числа 1..20 в отсортированном порядке. Выведи `height()`. Сравни с вставкой в случайном порядке.
2. Замерь время поиска в вырожденном BST vs случайном. Сравни с `std::map`.
3. Нарисуй (или напечатай) дерево после вставки 1,2,3,4,5 и после вставки 3,1,5,2,4.

### C. Сравнение с std::map (около 20 мин)

1. Вставь 100 000 элементов в BST и `std::map`. Сравни время.
2. Выполни 100 000 поисков. Объясни разницу в производительности.

### Самопроверка (около 15 мин, письменно в `selfcheck.md`)

1. Каков инвариант BST?
2. Три случая удаления — опиши каждый.
3. Почему отсортированный ввод ломает BST?
4. Чем AVL отличается от Red-Black Tree?
5. Когда использовать map vs unordered_map?

### Критерий "сделано"
BST работает корректно, is_valid() проходит, вырождение продемонстрировано, высота измерена.
