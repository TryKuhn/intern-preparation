# Intern preparation

Курс по подготовке к стажировке на C++ с нуля

# План

## Фаза 0 — Окружение и фундамент C++

### 0.1 Bash
- Навигация и файлы: `cd/ls/pwd/cp/mv/rm/mkdir`, абсолютные/относительные пути.
- Просмотр и поиск: `cat/less/head/tail`, `grep`, `find`, wildcards (`*`, `?`).
- **Regex (через `grep -E`/`egrep`)**: метасимволы `. * + ? [] ^ $ () |`, классы символов (`[0-9]`, `\d`, `\w`), якоря, группы и квантификаторы, жадность; basic vs extended regex; `sed`/`awk`.
- Пайпы и редиректы: `|`, `>`, `>>`, `2>&1`, `<`; цепочки `&&` / `||`.
- Права и исполнение: `chmod +x`, shebang `#!/bin/bash`; переменные окружения и `$PATH`.
- Процессы: `ps`, `top`, `kill`, фоновые задачи `&`.

### 0.2 Git
- Модель: рабочая директория + staging + commit + remote; что такое `HEAD`.
- База: `init/clone`, `add/commit/status/diff/log`, `.gitignore`.
- Ветки: `branch/switch/checkout`, **merge vs rebase**, разрешение конфликтов.
- Удалёнка: `push/pull/fetch`, PR-флоу на GitHub.
- Откаты: `restore`, `reset` (soft/mixed/hard), `revert` — и чем отличаются.
  
### 0.3 Тулчейн и модель сборки
- g++/clang, этапы: препроцессор + компиляция + линковка; объектные файлы.
- Базовый CMake, `gdb`, как читать ошибки компилятора и линкера.
- Тонкости: ODR (правило одного определения); `#pragma once`/header guards; форвард-декларации и неполные типы; `inline` (в т.ч. inline-переменные C++17) ; `static` = внутренняя линковка.
  
### 0.4 Память и типы
- Стек vs куча, как живёт переменная, `sizeof`, `size_t`.
- Тонкости: представление целых (доп. код), **знаковое переполнение = UB**; беззнаковое оборачивается; `-1 < 0u` → false; целочисленные промоушены; `size_t` - подтекание в циклах; знаковость `char` — impl-defined; float: `==`/NaN/точность.
  
### 0.5 Ссылки и указатели
- Передача по значению/ссылке/указателю; арифметика указателей, распад массива; `nullptr`.
- Тонкости: категории значений lvalue/rvalue (строго — glvalue/prvalue/xvalue); висячая ссылка; продление жизни temporary при `const&`; `auto` отбрасывает ссылки и верхний `const`, `decltype` — нет.
  
### 0.6 Динамическая память
- `new/delete`, утечки, висячие указатели, double-free.
- Тонкости: `delete` vs `delete[]`; **use-after-free**, dangling; начало/конец времени жизни объекта; placement new; выравнивание/паддинг, `alignof`/`alignas`.
  
### 0.7 RAII и умные указатели
- `unique_ptr`, `shared_ptr`, `weak_ptr`; кастомные делитеры.
- Тонкости: сырой указатель = невладеющий; `shared_ptr` — control block и **атомарный счётчик** (стоит денег); `weak_ptr` рвёт циклы, цикл shared_ptr = утечка; `make_shared` vs `new` — одна аллокация, weak держит блок; RAII/scope guard как защита от утечек при исключении; `enable_shared_from_this`.
  
### 0.8 const-корректность
- `const&` для передачи без копий, `const`-методы, `mutable`.
- Тонкости: верхний vs низкоуровневый `const`; `const int*` vs `int* const`; логическая vs битовая константность; `constexpr`/`consteval`/`constinit` (C++20); `volatile` — что делает и что **не** делает (не для потоков).
  
### 0.9 ООП ч.1 — классы и инициализация
- class/struct, инкапсуляция (private/public/protected), конструкторы/деструкторы, список инициализации.
- Тонкости: **порядок инициализации членов = порядок объявления**, не списка (варнинг); `explicit` и неявные преобразования; `{}`-инициализация и запрет сужения; ловушка `vector<int>{3}` vs `vector<int>(3)`; `auto x = {1,2}` -> `initializer_list`; делегирующие/наследуемые конструкторы; magic statics (потокобезопасная инициализация static-локалов с C++11); most vexing parse; SIOF.
  
### 0.10 ООП ч.2 — копирование и перемещение
- Тонкости: **rule of 0/3/5**; `=default`/`=delete`; copy vs move; `std::move` — **просто каст** к rvalue; rvalue-ссылка `&&`, moved-from состояние; copy elision / RVO / NRVO, обязательный elision с C++17; forwarding (universal) ссылка `T&&` vs rvalue-ссылка; свёртка ссылок (`& &&`→`&`, `&& &&`→`&&`); `std::forward` и perfect forwarding; **`noexcept` у move-конструктора -> `vector` переезжает, а не копирует** при реаллокации.
  
### 0.11 ООП ч.3 — наследование и полиморфизм
- Виртуальные функции, абстрактные классы, виртуальный деструктор.
- Тонкости: **vtable/vptr**; `override`/`final`; slicing; виртуальный деструктор — зачем; вызов виртуальной из конструктора/деструктора резолвится на текущий класс; **аргументы по умолчанию у виртуальных связываются статически**; сокрытие имён, `using` для подъёма перегрузок; ромб/виртуальное наследование.
  
### 0.12 Шаблоны (база) + тур по UB
- Шаблоны функций/классов, вывод аргументов, базовая специализация (нужны под свои контейнеры). **Глубина шаблонов вынесена в отдельный модуль 4.0 перед моками** — здесь только рабочий минимум.
- Тонкости: специализация (полная/частичная); `if constexpr` как простое ветвление. UB-тур: **UB vs unspecified vs implementation-defined**; порядок вычисления аргументов — unspecified, `i = i++` — UB; strict aliasing и опасность `reinterpret_cast`; четыре каста (static/dynamic/const/reinterpret).

## Контрольная работа №1. Основы C++, Git, Bash

## Фаза 1 — STL и свои реализации структур данных

### 1.1 Тур по STL + сложности
- vector, string, array, deque, list, map/set, unordered_map/set, stack, queue, priority_queue.
- Итераторы, `<algorithm>` (sort, lower_bound, unique).
- Таблица сложностей операций.
- Тонкости: **лямбды** (захват по значению/ссылке, init-capture C++14, `mutable`, generic, висячий `this`) — нужны для компараторов; `std::string_view` и его **висячесть**; `std::optional`/`std::variant`.
- **`std::regex`** (`<regex>`): `regex_match`/`regex_search`/`regex_replace`, группы захвата (`smatch`), грамматика ECMAScript — синтаксис уже знаком из `grep`, разбираем только API.
- **Важная оговорка:** `std::regex` заметно медленный и тяжёлый — годится для валидации/конфигов, не для горячих путей (там RE2/ручной парсинг).
  
### 1.2 Амортизированный анализ на примере vector
- Удвоение capacity, почему push_back амортизированно O(1).
- Своя реализация динамического массива.
   
### 1.3 Связные списки
- Свой singly/doubly linked list, вставка/удаление за O(1) по итератору.
   
### 1.4 Стек и очередь
- Своя реализация на массиве и на списке.
- Кольцевой буфер.

### 1.5 Хеш-таблица под капотом ч.1
- Хеш-функции, коллизии, **chaining vs open addressing**, load factor.
- Своя реализация с chaining.
   
### 1.6 Хеш-таблица под капотом ч.2.
- Рехеширование, амортизированно O(1), **худший случай O(n)**, hash-флуд.
    
### 1.7. Деревья поиска
- BST: вставка/поиск/удаление, вырождение
- Концептуально — балансировка (AVL/красно-чёрное), что использует `std::map`.
    
### 1.8 Куча
- Своя бинарная куча, `push`/`pop`/`heapify`, как устроен `priority_queue`.
    
### 1.9 Сводное занятие
- Когда какую структуру брать; обоснование выбора через сложности.
- Тонкости: перегрузка операторов (возврат `&` из `operator=`), `operator[]` vs `.at()`, spaceship `<=>` (C++20); ADL/поиск Кёнига.

## Контрольная работа №2. Структуры данных и STL

## Фаза 2 — Алгоритмы и структуры данных, ядро

### 2.1 Сложность и оценка
- Big-O/Θ/Ω, оценка по лимитам, прикидка «зайдёт ли».

### 2.2 Массивы/строки/
- Два указателя, скользящее окно.
- Префиксные суммы, разностный массив, частотные таблицы.

### 2.3 Сортировки
- merge/quick/heap своими руками, counting/radix, `std::sort`, стабильность, компараторы.

### 2.4 Бинарный поиск 
- По массиву, lower/upper bound, **бинпоиск по ответу**, баги границ.

### 2.5 Рекурсия и перебор
- Подмножества/перестановки, бэктрекинг, отсечения.

### 2.6 Стек/очередь
- Монотонный стек, скобочные последовательности.
### 2.7 Связные списки
- Разворот, цикл (Флойд), слияние.

### 2.8 Деревья 
- Обходы (pre/in/post, level-order), задачи на BST, LCA-интуиция.

### 2.9 Графы
- Представление, **BFS**, DFS, компоненты связности, топсорт, Дейкстра, DSU.

### 2.10 Динамика
- 1D (лестница, рюкзак), 2D, паттерны, восстановление ответа.

### 2.11 Жадность + математика
- Жадные доказательства, gcd, решето, модульная арифметика, **битовые операции**.

## Контрольная работа №3. АиСД

## Фаза 3 — Системный трек: концепты + мини-приложения на C++

### 3.1 Многопоточка в C++
- `std::thread`, `mutex`, `lock_guard`/`unique_lock`/`scoped_lock`, `condition_variable` (ложные пробуждения), `atomic`, **семафоры C++20** (`<semaphore>`).
- Тонкости: **гонка данных = UB**; модели памяти (relaxed/acquire-release/seq_cst); `thread_local`; `volatile` НЕ для потоков; дедлок и `std::lock`.
  
### 3.2 ОС/АКОС база\
- Процессы vs потоки, планировщик, виртуальная память, системные вызовы, endianness.
- **IPC (межпроцессное взаимодействие)**: пайпы (анонимные), FIFO (именованные пайпы), разделяемая память (`shmget`/`mmap`), семафоры (`semget`/`sem_open`), сигналы, сокеты (в т.ч. Unix domain).
- **Системные очереди сообщений: **System V** (`msgget`/`msgsnd`/`msgrcv`/`msgctl`, `mtype` для выборочного чтения) и **POSIX** (`mq_open`/`mq_send`/`mq_receive`/`mq_notify`, приоритеты, дескриптор-как-fd -> работает с `epoll`).
  
### 3.3 Сети и HTTP
- TCP/IP на пальцах, жизненный цикл HTTP-запроса, REST, статус-коды, идемпотентность.
- **Сериализация:** JSON vs **Protocol Buffers** (схема `.proto`, кодоген `protoc`, бинарный формат, varint, **эволюция схемы** — почему нельзя переиспользовать номера полей).
- **gRPC** как альтернатива REST: поверх HTTP/2, 4 типа вызовов (unary, server/client-streaming, bidirectional), дедлайны, метаданные; в C++ через `grpc++`.

### 3.4 Движки шаблонов
- Что это и зачем: отделение данных от представления, рендер переменных/циклов/условий в текст (HTML, конфиги, кодген).
- Канонический пример — **Jinja2** (Python). В C++ — **inja** (header-only, Jinja-подобный, на `nlohmann/json`), mustache/mstch.

### 3.5 БД — концепты + мини-апп
- Реляционная модель, SQL (PostgreSQL), CRUD, JOIN, **индексы (B-дерево)**, транзакции, **ACID**, уровни изоляции, нормализация.
- Мини-приложение: C++ + **libpqxx** к локальному Postgres.
  
### 3.6 Очереди сообщений — концепты + мини-апп
- Зачем очередь, **Kafka** (топики, партиции, consumer group, оффсеты), **RabbitMQ** (exchange/queue, AMQP), at-least/at-most/exactly-once
- Мини-приложение: producer/consumer на C++ + **librdkafka**.

### 3.7 Распределёнка и map-reduce
- Зачем, интуиция CAP, ментальная модель map-reduce, шардирование/репликация.

## Контрольная работа №4. Системный трек

### Фаза 4 — DevOps: Make / Docker / docker-compose / CI

### 4.1 Make и сборка
- Анатомия правила: `target: prereqs` + рецепт; автопеременные `$@`, `$<`, `$^`.
- `.PHONY` (и зачем), переменные, pattern rules (`%.o: %.cpp`).
- Инкрементальная сборка: как make решает, что пересобирать (по времени модификации).
- Связка с **CMake**: зачем CMake поверх make, что он генерирует (Makefile/Ninja), out-of-source build, `target_link_libraries`.
- Частые вопросы: чем `make` отличается от `cmake`; что такое `.PHONY`; почему таргет не пересобирается.
  
### 4.2 Docker
- **Образ vs контейнер vs реестр**; почему контейнер не виртуалка.
- **Слои и кэш сборки**: порядок инструкций в `Dockerfile` влияет на кэш.
- Инструкции: `FROM/RUN/COPY/WORKDIR/ENV/EXPOSE`, **`CMD` vs `ENTRYPOINT`**.
- **Multi-stage build под C++**: stage-сборщик с компилятором -> runtime-stage на slim-образе, кладём только бинарь; `.dockerignore`.
- Тома (volumes) для персистентности данных БД; проброс портов `-p`; переменные окружения.
- Частые вопросы: `CMD` vs `ENTRYPOINT`; зачем multi-stage; что такое слой; чем контейнер отличается от ВМ.

### 4.3 docker-compose и CI
- `docker-compose.yml`: `services`, `depends_on`, `networks`, `volumes`, `environment`, healthchecks.
- Поднять стек **«C++ сервис + Postgres + Kafka» одной командой**; как сервисы видят друг друга (DNS по имени сервиса).
- **CI**: GitHub Actions — триггеры (`on: push`/PR), jobs/steps, сборка + прогон тестов; что такое CI / CD / continuous deployment.
- Частые вопросы: зачем compose вместо нескольких `docker run`; как контейнеры общаются в одной сети; что происходит в пайплайне при пуше.

## Контрольная работа №5. DevOps

## Фаза 5 — Глубокие шаблоны, симуляции, резюме, заявка
 
### 5.0 Глубокие шаблоны и метапрограммирование
 
### 5.1 вывод, специализация, variadic
- Вывод аргументов в деталях: `T`, `T&`, `T&&` (forwarding) — связка со свёрткой ссылок и perfect forwarding; `decltype(auto)`.
- Специализация: полная vs частичная; **функции нельзя частично специализировать** — только перегружать (классическая ловушка).
- Variadic-шаблоны: parameter pack, раскрытие пакета, рекурсия vs **fold-выражения (C++17)**. Практика: `sum(...)`, `print(...)`.
- Non-type template parameters (NTTP), в т.ч. расширенные в C++20.

### 5.2 SFINAE, type traits своими руками
- Что такое SFINAE и зачем; `std::enable_if`, трюк `void_t`, detection idiom.
- Тур по `<type_traits>`: `is_same`, `is_integral`, `remove_reference`, `decay`, `conditional`, `is_base_of`.
- **Пишем свои трейты руками**: `integral_constant` / `true_type`/`false_type`, `is_same<T,U>`, `remove_const`, `remove_reference`, `conditional`. Как изнутри устроен `std::move` (шаблон, возвращающий rvalue-ссылку) и `std::forward`.
- Tag dispatch как классическая техника.
  
### 5.3 современная замена и модель инстанцирования
- **Concepts (C++20)**: `requires`-клаузы и `requires`-выражения, стандартные концепты, ограничение шаблонов — современная замена SFINAE.
- `if constexpr` против tag dispatch / SFINAE-ветвлений; `constexpr`-функции и вычисления на этапе компиляции (классический TMP — факториал — и почему `constexpr` его вытеснил).
- CRTP (статический полиморфизм) и где применяется; dependent names, дизамбигуаторы `typename`/`template`, two-phase lookup.
- **Модель инстанцирования**: где генерируется код, **почему шаблоны живут в заголовках**, explicit instantiation, code bloat.

## Контрольная работа №5. Вспомнить всё
 
### Фаза 6 — Симуляции и заявка
 
- **Контест-симуляции.** Полный формат 5 задач / 5 часов, несколько прогонов, тайм-менеджмент (сначала простые, не зависать).
- **Моки алго-секций.** Решение вслух, разбор кода без запуска, **блиц по тонкостям C++** (move, vtable, smart ptr, UB, hash map, noexcept).
- **Моки финалов.** Поведенческие + блиц по системным концептам + **DevOps/Git-вопросы** (Docker, multi-stage, merge vs rebase) + **API-протоколы** (protobuf vs JSON, gRPC vs REST);
